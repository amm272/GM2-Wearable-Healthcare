ecg = []
ppg = []

fsi = 200;
fsj = 200;

gr = 1;

[qrs_amp_raw,qrs_i_raw,delay]= peak(ppg,fsi,gr);
[qrs_amp_raw,qrs_j_raw,delay]= peak(ecg,fsj,gr);

size_array = size(qrs_i_raw);

for i = 1:size_array(2)
    a = qrs_i_raw(i)-qrs_j_raw(i);
    time(i) = a;
end

time
